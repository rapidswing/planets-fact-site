import { planets } from 'utils/constants'

export default function planetImage({ index }) {
  const getImage = () => {
    switch(index) {
      default:
        return 
    }
  }
  
  return (
    <div className="planet-image">

    </div>
  )
}